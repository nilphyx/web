export const metadata = {
  title: 'Frequently Asked Questions - AI Platform',
  description: 'Find answers to common questions about the AI Learning Platform, courses, data, and compute resources.',
};

export default function FaqsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 